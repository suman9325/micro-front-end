export default function Offline() {
  return (
    <div>
      <h1 className="h4 mb-2">Catalogue · Offline</h1>
      <p className="text-muted mb-0">Offline catalogue content (served from the catalogue remote).</p>
    </div>
  );
}
